repos:
  - repo: local
    hooks:
      - id: build-frontend
        name: build-frontend
        language: system
        entry: bash -c 'npm run build && npx @tailwindcss/cli -i src/rv/static/input.css -o src/rv/static/output.css && git add src/rv/static/bundle.js src/rv/static/output.css'
        always_run: true
        pass_filenames: false
        stages: [commit]
      - id: pyright
        name: pyright
        language: system
        entry: uv run pyright
        types: [python]
        pass_filenames: false
      # - id: pytest
      #   name: pytest
      #   language: system
      #   entry: uv run pytest
      #   always_run: true
      #   pass_filenames: false
